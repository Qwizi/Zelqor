[workspace]
resolver = "2"
members = [
    "zelqor-plugin-sdk",
    "speed-boost",
    "double-resources",
    "fog-of-war-extended",
    "chat-filter",
    "auto-balance",
]

[profile.release]
lto = true
opt-level = "s"
codegen-units = 1
panic = "abort"
strip = true
